// Entry point injected into every page (see manifest.json content_scripts).
//
// Responsibilities (see docs/ROADMAP.md Phase 1):
// - QC-10: detect page readiness before mounting the overlay
// - QC-11: text anchoring engine (quote + position + context)
// - QC-23: SVG overlay mount + reposition on scroll/resize
//
// Intentionally empty scaffold — implementation starts at QC-1 (anchoring spike).
